package common;

public class Config {
    public static final String PLATFORM_AND_BROWSER = "win_chrome";

    /**
     * Clear browser cookies and local storage each iteration
     * if true - clear cookies and local storage
     */
    public static final Boolean CLEAR_COOKIES_AND_STORAGE = true;
    /**
     * Keeps the browser window/tab opened
     * if true - keep the window opened
     */
    public static final Boolean KEEP_BROWSER_OPENED = true;
}
